apply plugin: "war"

bootWar {
    mainClassName = "com.fsd.sba.TechnologyApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
